# .openrouter-allowlist.yaml — pre-cleared OpenRouter-literal matches
# (alpha-engine-config-I7864, wiring the fleet-wide guard). Schema:
# nousergon-lib scripts/openrouter_guard.py::load_allowlist.
schema_version: 1
entries:
- path: .github/workflows/judge-perturbation-smoke.yml
  pattern: env_key
  reason: >-
    Fixed by alpha-engine-config-I7880: the secrets.OPENROUTER_API_KEY injection into the smoke step's
    env was removed (evaluate_artifact resolves its judge model via krepis.router, not this key). The
    remaining literal is historical prose in header comments explaining the migration and pointing at
    the still-open CI-router-reachability question (alpha-engine-config-I7853) — not a live call site.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7880
- path: agents/langchain_utils.py
  pattern: base_url
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: config.py
  pattern: env_key
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: infrastructure/deploy.sh
  pattern: env_key
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: lambda/openrouter_shadow_handler.py
  pattern: env_key
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: producers/single_agent.py
  pattern: base_url
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: producers/single_agent.py
  pattern: provider_literal
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: scripts/live_validate_openrouter_judge.py
  pattern: base_url
  reason: >-
    Fixed by alpha-engine-config-I7880: the script no longer constructs an openai.OpenAI client against
    https://openrouter.ai/api/v1 — it resolves through krepis.router.resolve_group_spec's low group like
    production judge call sites. The remaining literal is historical prose in the module docstring naming
    the removed direct-linkage pattern for context, not a live call site.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7880
- path: tests/live_smoke/judge_perturbation_smoke.py
  pattern: env_key
  reason: >-
    Fixed by alpha-engine-config-I7880: main() no longer gates on OPENROUTER_API_KEY presence or passes
    it into run_perturbation_battery (the judge call resolves its credential via krepis.router; passing
    a real key would have overridden that resolution). The remaining literal is historical prose in the
    module docstring and comments explaining the migration — not a live call site.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7880
- path: tests/live_smoke/judge_perturbation_smoke_openrouter.py
  pattern: env_key
  reason: >-
    Fixed by alpha-engine-config-I7880: main() no longer gates on OPENROUTER_API_KEY presence or passes
    it through to evaluate_artifact_openrouter, and now catches a router-unreachable ValueError as a SKIP
    the same way its sync-primary sibling does. The remaining literal is historical prose in the module
    docstring — not a live call site. Not CI-wired today (manual/ad-hoc only per its own docstring).
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7880
- path: tests/test_agent_llm_factory.py
  pattern: base_url
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: tests/test_agent_llm_factory.py
  pattern: env_key
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: tests/test_agent_llm_factory.py
  pattern: provider_literal
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: tests/test_eval_judge_openrouter.py
  pattern: env_key
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: tests/test_eval_judge_router_addressing.py
  pattern: env_key
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: tests/test_eval_judge_router_addressing.py
  pattern: provider_literal
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: tests/test_no_secret_environ_reads.py
  pattern: env_key
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: tests/test_single_agent_producer.py
  pattern: env_key
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: tests/test_thinktank_group_addressing.py
  pattern: env_key
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: tests/test_thinktank_group_addressing.py
  pattern: provider_literal
  reason: >-
    Historical/test prose describing the direct-OpenRouter class this guard exists to catch (comment,
    docstring, or a test asserting the literal's ABSENCE) — not a live call site. Baseline sweep for alpha-engine-config-I7864.
  expires: '2026-11-18'
  tracking: alpha-engine-config-I7864
- path: .provider-linkage-allowlist.yaml
  pattern: base_url
  reason: >-
    This session's new fleet allowlist file names openrouter.ai in its own reason prose, explaining a
    historical migration — not a live call site. Baseline sweep for alpha-engine-config-I9302.
  expires: '2026-11-28'
  tracking: alpha-engine-config-I9302
- path: .provider-linkage-allowlist.yaml
  pattern: env_key
  reason: >-
    Same file, reason prose names OPENROUTER_API_KEY literally in several entries explaining historical/out-of-scope
    matches — not a live call site. Baseline sweep for alpha-engine-config-I9302.
  expires: '2026-11-28'
  tracking: alpha-engine-config-I9302
- path: tests/test_thinktank_no_direct_provider_linkage.py
  pattern: provider_literal
  reason: >-
    New regression test's fixture data asserting the retired pinned-provider schema ({"provider": "openrouter",
    ...}) is REJECTED at parse — the literal proves the schema no longer parses, it is not a live call
    site. Confirmed RED pre-fix / GREEN post-fix.
  expires: '2026-11-28'
  tracking: alpha-engine-config-I9302
