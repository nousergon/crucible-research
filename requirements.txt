# Core
# setuptools floor for PYSEC-2026-3447 (newly disclosed; CI's pip-audit gate
# started failing 2026-07-14 against the default 79.0.1 bundled by the
# GH Actions Python 3.11 image). Not a build-backend requirement here — direct
# pin exists solely to satisfy the security-audit gate.
setuptools>=83.0.0
anthropic~=0.120
# Raised to the 1.x line (config#2570): langchain-anthropic~=1.5 forces
# langchain-core 1.x, and the transitive eval graph (agentevals~=0.0 ->
# openevals -> langchain 1.x) already resolves to langchain-core>=1.4.9 +
# langgraph>=1.2.5. langgraph <1.2.5 (the 0.x lines) pins langchain-core<1.0
# — uninstallable alongside 1.x (pip dies with `resolution-too-deep`
# backtracking every 0.x release). `~=1.2` tracks the line the eval graph
# already pulls in.
langgraph~=1.2
langchain-anthropic~=1.5
# OpenAI-compatible chat model — how every research agent now reaches the
# router (or OpenRouter in Lambda). See agents/langchain_utils.make_agent_llm.
# Raised to the 1.x line (config#2570): the transitive eval graph
# (agentevals~=0.0 -> openevals -> langchain 1.x) already resolves
# langchain-openai to 1.4.1; a `<1.0` cap fights that and leaves the stale
# 0.3.x compat line pinned under langchain-core 1.x. The 1.4.x line is the
# current path.
langchain-openai~=1.5
# Bumped 0.7 -> 0.8.18 (alpha-engine-config#2522): GHSA-f4xh-w4cj-qxq8, HIGH —
# TracingMiddleware arbitrary server-side file read, fixed in 0.8.18. The old
# `~=0.7` bound (PEP 440: fixes only the leading "0", i.e. >=0.7,<1.0) let
# Dependabot's manifest scan flag the range as covering vulnerable <0.8.18
# builds even though a fresh resolve already picked a newer version; `~=0.8.18`
# hard-excludes every pre-fix release while staying within the 0.8.x line
# (minimum version movement that clears the advisory).
langsmith~=0.10.18
# openai: the OpenAI-compatible wire client for thinktank/ (config#1579) —
# talks to OpenRouter / self-hosted vLLM / any OpenAI-compatible endpoint via
# base_url. NOT used by the weekly research pipeline (which stays on
# langchain-anthropic); floor (not ~=) so provider SDK churn doesn't gate us.
openai>=2.53.0

# Evals
agentevals~=0.0

# Data
yfinance~=1.6.0
requests-ratelimiter~=0.10
pandas~=2.2
numpy~=1.26
requests~=2.34
feedparser~=6.0.13
beautifulsoup4~=4.15
# Bumped 5.2 -> 6.1 (config#2342): PYSEC-2026-87 (untrusted XML entity
# resolution could read local files under the old resolve_entities=True
# default; lxml 6.1.0 flips the default to resolve_entities='internal').
# This repo only touches lxml indirectly as BeautifulSoup's parser backend
# (data/fetchers/news_fetcher.py) — no direct lxml.etree/XML-parsing calls
# of our own — so the 5->6 API surface change carries low local risk.
lxml~=6.1
# defusedxml (config#2532): hardened drop-in for xml.etree.ElementTree —
# data/fetchers/insider_fetcher.py parses Form 4 XML fetched live from SEC
# EDGAR (network response, not a local/trusted literal), so stdlib
# ElementTree's unguarded entity resolution is a real XXE surface even
# though the source is a reputable government endpoint.
defusedxml~=0.7.1

# Config / storage
pyyaml~=6.0.3
boto3~=1.43
pydantic~=2.13
# alpha-engine-lib provides setup_logging, BasePreflight, and (since v0.3.0)
# the rag submodule consolidating db/embeddings/retrieval/schema previously
# duplicated in this repo's rag/ folder.
# Extras:
#   [arcticdb]    arcticdb (data/fetchers/price_fetcher.py)
#   [flow-doctor] flow-doctor (lambda/handler.py setup_logging integration)
#                 (HYPHENATED: setuptools normalizes the lib's `flow_doctor`
#                 extra to `flow-doctor`; old pip drops `[flow_doctor]` — config#1178)
#   [rag]         psycopg2-binary + pgvector + numpy (qual_tools query_filings,
#                 graph/research_graph rag.is_available probe)
# Lib is now public — no PAT required. Lambda Dockerfile installs via the
# git URL; CI pulls directly.
# Tradeability cost engine (config#1399): nousergon_lib.quant.transaction_cost,
# shipped in nousergon-lib v0.75.0 (#144 merged + tagged 2026-06-30).
# Bumped 0.75.0 -> 0.78.0 (config#1483/config#1530): the research/episodic
# consumer cutover onto the long-format score_performance_outcomes store
# needs nousergon_lib.quant.horizons (HorizonPolicy/OutcomeColumns, lib#147,
# v0.77.0) + nousergon_lib.quant.horizon_guard (the shared burn-down-guard
# testing primitive lifted on the second adoption, lib#149, v0.78.0) —
# this repo adopts the shared guard directly rather than hand-rolling its
# own copy (see tests/test_wide_horizon_column_burndown_guard.py).
# Also satisfies the research_intel contract (config#1500): the
# research_intel.schema.json + SLOT_SCHEMAS["research_intel"] registration
# shipped in v0.77.0 and are still present in v0.78.0 (additive bump —
# verified). test_lib_pin_lockstep keeps all three deploy artifacts (this
# file + Dockerfile + Dockerfile.alerts) in lockstep.
# Bumped 0.78.0 -> 0.79.0 (config#1539): the SFT provenance chokepoint
# (nousergon-lib#150) — typed SftProvenance (source live|replay|synthetic +
# content_hash), build_record(source=) auto-stamp, and corpus dedup/segregate
# utils. Additive + backward-compatible (build_record defaults source="live" +
# schema_version 3), so this repo's two SFT producers (graph/llm_cost_tracker,
# thinktank/client) are born correctly tagged and the distillation corpus reader
# (scripts/corpus_stats.py) can segregate by (producer, task, teacher, source)
# without blending replay-minted traces into live-accrued ones.
# Bumped 0.83.0 -> 0.86.0 (config#1727 Phase C): migrate health enrichment
# writes to nousergon_lib.health (Deliverable + derive_status + write_health).
# Cross-sectional 6-pillar attractiveness chokepoint (nousergon_lib.quant.attractiveness,
# lib#164 / v0.88.0) — shared with Metron Holdings + universe_board backfill.
# Bumped 0.88.0 -> 0.124.3 (alpha-engine-config#3077 Phase C): needs the
# experiment_record.v1 + experiment.v1 contract schemas (nousergon-lib#172,
# shipped v0.94.0) for the new per-challenger experiment_record emission
# (producers/experiment_record.py). Matches crucible-evaluator's pin exactly
# (same schema version fleet-wide). The only breaking change in this range
# (nousergon-lib#197, v0.113.0: removed the deprecated alpha_engine_lib ->
# nousergon_lib import alias shim) had exactly one straggler left in this
# repo — scoring/boost_signals.py's `from alpha_engine_lib.secrets import
# get_secret` — fixed in the same PR to `from nousergon_lib.secrets import
# get_secret` (matching every other call site in this repo already).
nousergon-lib[arcticdb,flow-doctor,rag,contracts] @ git+https://github.com/nousergon/nousergon-lib@v0.124.3
# krepis floor: graph/llm_cost_tracker.py reads the operator pricing override
# (cost/model_pricing.yaml) via alpha_engine_lib.cost -> krepis.cost.PriceCard,
# which is extra="forbid". config#1332 adds the cache_create_1h_per_1m field,
# present only in krepis>=0.6.0 — an older krepis rejects the YAML. Pinned
# directly (not via a nousergon-lib bump, which would drag the in-flight,
# 7/3-cutover SF-rename refs in lib 0.74.0 into this repo). Floor guarantees
# the upgrade even on a non-fresh venv.
#
# Bumped to >=0.7.0 (config#1494 + config#1545): deploy.sh's canary now CALLS
# the `krepis.aws invoke-canary` CLI (0.7.0 ships it) instead of an inline Bash
# throttle-retry helper. The invoke is now LOAD-BEARING (not best-effort like
# the alerts call), so krepis must be importable in the deploy environment —
# this floor makes it a hard runtime/deploy dependency.
#
# Bumped to >=0.13.0 (config#1675 / config#2575): evals/judge.py +
# evals/judge_models.py now delegate their transport/mechanics layer
# (rubric rendering, structured-output tool spec + batch-result parse,
# custom_id codec, judge-model registry) to krepis.judge, which ships
# starting 0.13.0. (krepis#27 merged 2026-07-15, published as krepis==0.13.0.)
#
# Bumped again to >=0.16.0 (config#2575 items 2/3, 2026-07-18):
# evals/judge.py's new evaluate_artifact_openrouter (the OpenRouter
# shadow-judge tier) needs krepis.judge.check_openai_tool_response_for_leak
# + JudgeToolCallLeakError (the judge-tier-specific tool-call leak guard),
# which ship starting 0.16.0. BLOCKED until the companion krepis PR merges
# + PyPI publish completes — same gate:dependency linkage pattern as the
# 0.13.0 bump above.
# Bumped to >=0.25.0 (alpha-engine-config#5223 / #5719, 2026-07-30): this
# migration routes thinktank + the judge through krepis.llm, and 0.25.0 is the
# first release where that library is a SUPERSET of the fork it replaces —
# krepis#93 added the null-choices guard at all five `choices[0]` reads and
# bounded backoff between body-level retries. On 0.24.x this migration is a
# REGRESSION: measured 2026-07-30, a null-choices body raised
# `TypeError: 'NoneType' object is not subscriptable` through the migrated
# client, reintroducing the bug tests/test_null_choices_provider_error.py
# exists to prevent. The floor is load-bearing, not hygiene.
# 0.31.0 adds `krepis.router.resolve_group_spec` (group -> ModelSpec, the
# adapter every consumer was hand-writing) and KREPIS_ROUTER_CREDENTIAL_SECRET
# (per-consumer router identity — the edge identifies a consumer BY its
# credential value and krepis.secrets resolves SSM before os.environ, so two
# consumers sharing a secret NAME collapse into one identity there). Both are
# load-bearing for alpha-engine-config-I6367: this repo's thinktank tiers and
# the single_agent_quant challenger now address model groups instead of
# pinning provider="openrouter".
# The floor is >=0.31.1 (krepis-PR107), NOT 0.31.0: 0.31.0 emitted the proxy
# route with `provider: "litellm"`, which ModelSpec maps to TRANSPORT_LITELLM
# — krepis' IN-PROCESS Router, which calls each provider directly from the
# consumer and reads OPENROUTER_API_KEY out of the environment. That would
# send this repo's Lambda straight to openrouter.ai, unscanned. 0.31.1 emits
# `provider: "litellm_proxy"` (a name unknown to PROVIDER_REGISTRY), so
# ModelSpec treats the edge as the custom OpenAI-compatible endpoint it is —
# the chain is walked by the proxy, server-side. An earlier krepis reopens
# the I6183 failure mode (scanned-route logging while egressing unscanned).
# 0.31.2 raises the AppConfig registry-resolution failure from DEBUG to
# WARNING. Load-bearing here, not cosmetic: krepis.logging pins the root
# logger at INFO with no override, so on 0.31.1 that failure is unobservable
# and surfaces only as a much later "LLM_MODEL_REGISTRY.yaml not found"
# naming neither AppConfig nor the cause -- and suggesting a remedy
# (LLM_MODEL_REGISTRY_PATH) that is wrong for a public-repo Lambda.
# Measured 2026-08-04 on alpha-engine-research-runner: all six KREPIS_ vars
# set, IAM simulating allowed, the same call succeeding from a laptop, and
# AppConfig resolution still failing on every invocation
# (alpha-engine-config-I6373).
# Bumping the pin also changes this file's hash, which is what forces the
# Docker layer to rebuild -- an unchanged requirements.txt reuses the cached
# pip layer and keeps shipping 0.31.1 however many times the deploy runs.
#
# >=0.34.0 (alpha-engine-config-I6373, 2026-08-04) is what makes the Think Tank
# box and the research Lambda able to CALL the router they are already admitted
# to. Both address model groups through the edge on a per-consumer credential
# whose value lives in SSM at /alpha-engine/ROUTER_CONSUMER_{THINKTANK,RESEARCH}
# -- deliberately, so the secret never enters a Lambda config, an environment,
# a CloudWatch log, or the dispatcher's SSM command string. Through 0.33.0
# `LLMClient._resolve_api_key` read `os.environ` alone, so both consumers were
# ADMITTED to the route (route admission resolves the same credential on the
# full chain) and then died at the call:
#
#   LLMConfigError: no API key for provider 'litellm_proxy': pass api_key= or
#   set the ROUTER_CONSUMER_THINKTANK environment variable
#
# Measured on the daily run of 2026-08-04 (thinktank/runs/2026-08-04/
# manifest_1d6e7a653137.json): aborted after 5s, 0 theses, total_cost_usd 0,
# challenger_selection unwritten -- with all six KREPIS_ vars set on the box
# and the SSM parameter present and readable. The floor is what restores the
# Think Tank, not hygiene.
#
# NOT >=0.33.0, which is the obvious read of krepis-PR113 (config-I6414) but is
# wrong: PR113 fixed the ADMISSION half of this seam and left `llm.py`
# byte-identical between 0.32.0 and 0.33.0. On 0.33.0 the box fails exactly as
# it did on 0.32.0.
#
# 0.36.0 (config-I6652, krepis-PR115): `_resolve_group_served_model` — a
# capability-GROUP alias must never be reported as the model that served. On
# 0.34.0 and 0.35.0, `complete()`'s litellm_proxy branch and
# `_structured_litellm()` fell back to `self.spec.model` when the response's
# `model` field came back empty, so the literal string "low" reached
# `thinktank/client.py::_cost_for`, found no price card, and RAISED -- correct
# fail-loud behaviour on a value that should never have been produced.
# Measured live on 2026-08-04 (thinktank/runs/2026-08-04/manifest_df20e6c66665
# .json): 0 theses written.
#
# The floor is a CONTRACT, not a hope. A fresh spot box resolves to the newest
# release and would pick up 0.36.0 anyway -- that is resolution order, not a
# requirement, and a cached layer, a lockfile, or the versioned base AMI planned
# in config-I6165 can each serve 0.35.0 and silently reintroduce the defect.
# `tests/test_thinktank_group_addressing.py::
# TestGroupAliasIsNeverReportedAsTheServedModel` is the half that verifies the
# CAPABILITY rather than the version string, mirroring the credential contract
# added for the 0.34.0 floor directly above it in that file.
# 0.43.0 floor: `setup_logging(flow_name=...)`, which `lambda/thinktank_handler.py`
# uses to file this component's alerts under `thinktank-spot` rather than the
# repo-wide `research-lambda` in flow-doctor.yaml (alpha-engine-config-I6910).
# An older krepis raises TypeError on the kwarg rather than degrading, so the
# floor is load-bearing rather than advisory.
#
# 0.51.0 floor (krepis-PR133, alpha-engine-config-I7009): `krepis.ec2_spot
# relaunch-decision` gained `--json`, putting the verdict on a JSON field
# instead of the exit code. `infrastructure/spot_research_weekly.sh` reads
# that field; an older krepis has no `--json` flag at all.
#
# 0.59.4 floor (alpha-engine-config-I7214): the FIRST published krepis
# carrying `krepis.stage_coverage`, which every weekly-SF-backed handler in
# lambda/ imports to self-assert its declared output before returning. The
# call site's ImportError guard is deliberately silent about the handler's
# own outcome (observe mode), so a deploy resolving an older krepis would
# leave the assertion emitting NOTHING — indistinguishable on the console
# from an assertion that ran and found nothing wrong (config-I7334). The
# floor is the only thing that makes it measurable where it runs.
#
# 0.59.6 floor (alpha-engine-config-I7372): the FIRST published krepis
# carrying `krepis.spot_bootstrap`'s `--max-runtime-seconds` flag and
# `scan_for_inline_bootstraps`. `infrastructure/spot_research_weekly.sh`'s
# bootstrap step passes `--max-runtime-seconds`; an older krepis rejects it
# as an unrecognized argument — AFTER the spot instance already exists and
# is billing, not at dispatch. `tests/test_spot_bootstrap_invariants.py`
# imports `scan_for_inline_bootstraps`, which does not exist below 0.59.6 —
# `ImportError` at collection on an older resolve (measured live on
# crucible-predictor-PR505 against this exact symbol). A `>=0.59.4` floor
# lets `pip` resolve past both silently whenever the newest publish happens
# to satisfy it, which is resolution luck, not a requirement.
krepis>=0.59.6

# Scheduling / calendar
exchange-calendars~=4.13
pytz~=2026.3

# DB
# SQLite is stdlib; no extra package needed

# Dev / testing
pytest~=9.1
pytest-asyncio~=1.4
# Bumped 1.0.1 -> 1.2.2 (config#2342): CVE-2026-28684, set_key()/unset_key()
# followed symlinks when rewriting .env files (arbitrary local file
# overwrite via crafted symlink + cross-device rename fallback). 1.2.2 is a
# same-major (1.x) patch/minor bump, no API changes used by this repo.
python-dotenv~=1.2.2
# moto: mocked S3 for decision_capture integration tests
moto~=5.2

# SEC EDGAR data
edgartools~=5.48

# RAG — vector retrieval over SEC filings + earnings transcripts
psycopg2-binary~=2.9
voyageai~=0.5
